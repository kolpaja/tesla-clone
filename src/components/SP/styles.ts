import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    alignSelf: 'center',
    justifyContent: 'center',
    top: 40,
    zIndex: 2,
    color: '#a1a1a1',
  },
  text: {
    width: 32,
    height: 32,
    fontSize: 16,
    color: '#a1a1a1',
    textAlign: 'center',
    fontWeight: 'bold',
    padding: 5,
    borderRadius: 16,
    borderColor: '#a1a1a1',
    borderWidth: 1,
  },
});
